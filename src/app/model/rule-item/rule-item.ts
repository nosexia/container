export interface RuleItem {
    ruleName: String,
    confId: Number,
    enterpriseId: Number,
    sensorName: String,
    sensorTag: Number,
    ruleTag: String,
    ruleLen: String,
    ruleValue: String,
    unit: String,
    checked: Boolean,
    disabled: Boolean
}
